/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  AccountMeta,
  Context,
  PublicKey,
  Serializer,
  Signer,
  TransactionBuilder,
  mapSerializer,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import { addObjectProperty, isWritable } from '../shared';

// Accounts.
export type RefundInstructionAccounts = {
  /** Fair auction account */
  fairAuction: PublicKey;
  /** Bid account (pda of ['bid', fair auction id, owner id] */
  bid: PublicKey;
  /** Owner */
  owner: Signer;
  /** Payer */
  payer?: Signer;
};

// Data.
export type RefundInstructionData = { discriminator: number };

export type RefundInstructionDataArgs = {};

export function getRefundInstructionDataSerializer(
  context: Pick<Context, 'serializer'>
): Serializer<RefundInstructionDataArgs, RefundInstructionData> {
  const s = context.serializer;
  return mapSerializer<RefundInstructionDataArgs, any, RefundInstructionData>(
    s.struct<RefundInstructionData>([['discriminator', s.u8()]], {
      description: 'RefundInstructionData',
    }),
    (value) => ({ ...value, discriminator: 3 })
  ) as Serializer<RefundInstructionDataArgs, RefundInstructionData>;
}

// Instruction.
export function refund(
  context: Pick<Context, 'serializer' | 'programs' | 'payer'>,
  input: RefundInstructionAccounts
): TransactionBuilder {
  const signers: Signer[] = [];
  const keys: AccountMeta[] = [];

  // Program ID.
  const programId = {
    ...context.programs.getPublicKey(
      'mplGavel',
      'FairV2mqxnkNjniBpESKcF9u2sWrD3uLaP8eXcQHAgXh'
    ),
    isWritable: false,
  };

  // Resolved inputs.
  const resolvingAccounts = {};
  addObjectProperty(resolvingAccounts, 'payer', input.payer ?? context.payer);
  const resolvedAccounts = { ...input, ...resolvingAccounts };

  // Fair Auction.
  keys.push({
    pubkey: resolvedAccounts.fairAuction,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.fairAuction, false),
  });

  // Bid.
  keys.push({
    pubkey: resolvedAccounts.bid,
    isSigner: false,
    isWritable: isWritable(resolvedAccounts.bid, true),
  });

  // Owner.
  signers.push(resolvedAccounts.owner);
  keys.push({
    pubkey: resolvedAccounts.owner.publicKey,
    isSigner: true,
    isWritable: isWritable(resolvedAccounts.owner, false),
  });

  // Payer.
  signers.push(resolvedAccounts.payer);
  keys.push({
    pubkey: resolvedAccounts.payer.publicKey,
    isSigner: true,
    isWritable: isWritable(resolvedAccounts.payer, true),
  });

  // Data.
  const data = getRefundInstructionDataSerializer(context).serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
